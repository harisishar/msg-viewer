---
phase: 02-eml-parser-core
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tests/eml-parser.test.ts
  - tests/fixtures/simple.eml
  - tests/fixtures/multipart.eml
  - tests/fixtures/multipart-mixed.eml
  - tests/fixtures/encoded-headers.eml
autonomous: true
requirements:
  - EML-01
  - EML-02
  - EML-03
  - EML-04
  - EML-05
  - EML-06
  - EML-07
  - EML-08
  - EML-09
  - EML-10
  - SEC-01
  - SEC-02

must_haves:
  truths:
    - "Tests prove parseEml returns correct UnifiedMessage shape for a simple .eml"
    - "Tests prove headers are correctly mapped (Subject, From, To, CC, Date)"
    - "Tests prove HTML body is sanitized (script tags removed)"
    - "Tests prove remote images are blocked (https src stripped)"
    - "Tests prove plain text fallback works when no HTML part"
    - "Tests prove attachments are populated for multipart/mixed emails"
    - "Tests prove graceful degradation for empty/malformed input"
  artifacts:
    - path: "tests/eml-parser.test.ts"
      provides: "Test suite for parseEml"
      min_lines: 90
    - path: "tests/fixtures/simple.eml"
      provides: "Basic test fixture with headers and plain text body"
    - path: "tests/fixtures/multipart.eml"
      provides: "Multipart/alternative fixture with HTML and plain text"
    - path: "tests/fixtures/multipart-mixed.eml"
      provides: "Multipart/mixed fixture with text body and base64-encoded attachment"
    - path: "tests/fixtures/encoded-headers.eml"
      provides: "Fixture with RFC 2047 encoded Subject"
  key_links:
    - from: "tests/eml-parser.test.ts"
      to: "lib/scripts/eml/eml-parser.ts"
      via: "import parseEml"
      pattern: "import.*parseEml.*eml-parser"
---

<objective>
Create test fixtures and a comprehensive test suite that validates all EML parser requirements against real .eml content.

Purpose: Prove that parseEml() correctly handles standard emails, multipart structures, encoded headers, HTML sanitization, attachment extraction, and edge cases. This validates all 12 phase requirements.

Output: Passing test suite with .eml fixtures covering all requirement IDs.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-eml-parser-core/02-CONTEXT.md
@.planning/phases/02-eml-parser-core/02-01-SUMMARY.md
@.planning/phases/02-eml-parser-core/02-02-SUMMARY.md

<interfaces>
From lib/scripts/eml/eml-parser.ts:
```typescript
export async function parseEml(source: string | ArrayBuffer): Promise<UnifiedMessage>
```

From lib/scripts/types/unified-message.ts:
```typescript
export interface UnifiedMessage {
  content: UnifiedMessageContent;
  attachments: UnifiedAttachment[];
  recipients: UnifiedRecipient[];
}

export interface UnifiedMessageContent {
  date: Date | null;
  subject: string;
  senderName: string;
  senderEmail: string;
  body: string;
  bodyHTML: string;
  bodyRTF?: DataView;
  headers: string;
}

export interface UnifiedRecipient {
  name: string;
  email: string;
  type: 'to' | 'cc' | 'bcc';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .eml test fixtures</name>
  <files>tests/fixtures/simple.eml, tests/fixtures/multipart.eml, tests/fixtures/multipart-mixed.eml, tests/fixtures/encoded-headers.eml</files>
  <read_first>
    - /Users/abu/Documents/Projects/msg-viewer/.planning/phases/02-eml-parser-core/02-CONTEXT.md
  </read_first>
  <action>
Create the `tests/fixtures/` directory and four .eml test files. Each file must be valid RFC 822 format with CRLF line endings (\r\n).

**1. `tests/fixtures/simple.eml`** — Basic plain-text email:
```
From: John Doe <john@example.com>
To: Jane Smith <jane@example.com>
CC: Bob <bob@example.com>
Subject: Test Email Subject
Date: Mon, 01 Apr 2026 10:00:00 +0000
MIME-Version: 1.0
Content-Type: text/plain; charset=utf-8
Content-Transfer-Encoding: 7bit

This is the plain text body of the test email.
```

**2. `tests/fixtures/multipart.eml`** — Multipart/alternative with HTML and plain text, including XSS attack vectors:
```
From: Sender Name <sender@example.com>
To: recipient@example.com
Subject: Multipart Test
Date: Tue, 02 Apr 2026 12:00:00 +0000
MIME-Version: 1.0
Content-Type: multipart/alternative; boundary="boundary123"

--boundary123
Content-Type: text/plain; charset=utf-8

Plain text version of the email.

--boundary123
Content-Type: text/html; charset=utf-8

<html><body>
<p>Hello <b>World</b></p>
<table><tr><td>Cell</td></tr></table>
<script>alert('xss')</script>
<img src="https://tracker.example.com/pixel.gif" alt="tracking">
<img src="data:image/png;base64,iVBOR" alt="inline">
<a href="javascript:alert('xss')">Click</a>
<a href="https://example.com">Safe Link</a>
</body></html>

--boundary123--
```

**3. `tests/fixtures/multipart-mixed.eml`** — Multipart/mixed with a text/plain body and a small base64-encoded text attachment (for EML-07 attachment extraction testing):
```
From: Sender Name <sender@example.com>
To: recipient@example.com
Subject: Email With Attachment
Date: Thu, 03 Apr 2026 09:00:00 +0000
MIME-Version: 1.0
Content-Type: multipart/mixed; boundary="mixedboundary456"

--mixedboundary456
Content-Type: text/plain; charset=utf-8

This email has an attachment.

--mixedboundary456
Content-Type: text/plain; name="hello.txt"
Content-Transfer-Encoding: base64
Content-Disposition: attachment; filename="hello.txt"

SGVsbG8sIFdvcmxkIQ==

--mixedboundary456--
```

The base64 value `SGVsbG8sIFdvcmxkIQ==` decodes to `Hello, World!`. This gives the attachment test a real, verifiable payload.

**4. `tests/fixtures/encoded-headers.eml`** — RFC 2047 encoded Subject with international characters:
```
From: =?UTF-8?B?0JjQstCw0L0=?= <ivan@example.com>
To: recipient@example.com
Subject: =?UTF-8?Q?Re:_Caf=C3=A9_menu_=E2=80=93_updated?=
Date: Wed, 03 Apr 2026 14:00:00 +0000
MIME-Version: 1.0
Content-Type: text/plain; charset=utf-8

Email with encoded headers.
```

IMPORTANT: Use actual CRLF line endings. Write these files with `\r\n` line breaks. The blank line between headers and body is mandatory (RFC 822).
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Projects/msg-viewer && node -e "
const fs = require('fs');
const files = ['tests/fixtures/simple.eml', 'tests/fixtures/multipart.eml', 'tests/fixtures/multipart-mixed.eml', 'tests/fixtures/encoded-headers.eml'];
let ok = true;
for (const f of files) {
  if (!fs.existsSync(f)) { console.error('MISSING: ' + f); ok = false; continue; }
  const content = fs.readFileSync(f, 'utf8');
  if (!content.includes('From:')) { console.error('NO From: header in ' + f); ok = false; }
  if (!content.includes('Subject:')) { console.error('NO Subject: header in ' + f); ok = false; }
  console.log('OK: ' + f);
}
const mixed = fs.readFileSync('tests/fixtures/multipart-mixed.eml', 'utf8');
if (!mixed.includes('multipart/mixed')) { console.error('multipart-mixed.eml missing multipart/mixed content-type'); ok = false; }
if (!mixed.includes('Content-Disposition: attachment')) { console.error('multipart-mixed.eml missing attachment part'); ok = false; }
if (!ok) process.exit(1);
console.log('All fixtures exist and have basic headers');
"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/simple.eml` exists with From, To, CC, Subject, Date headers and plain text body
    - `tests/fixtures/multipart.eml` exists with multipart/alternative boundary, text/plain and text/html parts, includes `<script>` tag and remote `https://` image src and `javascript:` href for XSS testing
    - `tests/fixtures/multipart-mixed.eml` exists with Content-Type multipart/mixed, a text/plain body part, and a base64-encoded attachment with Content-Disposition: attachment
    - `tests/fixtures/encoded-headers.eml` exists with RFC 2047 =?UTF-8?Q?...?= encoded Subject and =?UTF-8?B?...?= encoded From name
    - All files have CRLF line endings
  </acceptance_criteria>
  <done>Four .eml test fixtures created covering simple, multipart/alternative, multipart/mixed with attachment, and encoded-header scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Create test suite for parseEml</name>
  <files>tests/eml-parser.test.ts</files>
  <read_first>
    - /Users/abu/Documents/Projects/msg-viewer/lib/scripts/eml/eml-parser.ts
    - /Users/abu/Documents/Projects/msg-viewer/lib/scripts/types/unified-message.ts
    - /Users/abu/Documents/Projects/msg-viewer/tests/fixtures/simple.eml
    - /Users/abu/Documents/Projects/msg-viewer/tests/fixtures/multipart.eml
    - /Users/abu/Documents/Projects/msg-viewer/tests/fixtures/multipart-mixed.eml
    - /Users/abu/Documents/Projects/msg-viewer/tests/fixtures/encoded-headers.eml
  </read_first>
  <behavior>
    - EML-01: parseEml(simpleEml) returns object with content, attachments, recipients
    - EML-02: content.subject === 'Test Email Subject', content.senderName === 'John Doe', content.senderEmail === 'john@example.com', content.date is valid Date
    - EML-03: parseEml(encodedEml).content.subject contains decoded 'Cafe menu' (not raw =?UTF-8?Q?...)
    - EML-04: parseEml(simpleEml).content.body contains 'plain text body'
    - EML-05: parseEml(multipartEml).content.bodyHTML contains '<b>World</b>' (preserved)
    - EML-06: parseEml(multipartEml).content.bodyHTML is non-empty (HTML preferred), content.body also available as fallback
    - EML-07: parseEml(multipartMixedEml).attachments has length >= 1, attachments[0].filename === 'hello.txt'
    - SEC-01: parseEml(multipartEml).content.bodyHTML does NOT contain '<script' or 'javascript:'
    - SEC-02: parseEml(multipartEml).content.bodyHTML does NOT contain 'https://tracker.example.com' but DOES contain 'data:image/png' img
    - Graceful degradation: parseEml('') or parseEml with empty body does not throw
  </behavior>
  <action>
Create `tests/eml-parser.test.ts` using Bun's built-in test runner (this project uses Bun).

```typescript
import { describe, test, expect } from 'bun:test';
import { readFileSync } from 'fs';
import { parseEml } from '../lib/scripts/eml/eml-parser';
```

**NOTE on DOMPurify in tests:** DOMPurify requires a DOM environment. Bun tests run in a jsdom-like environment via `bun test`. If DOMPurify fails in test env, the tests for SEC-01 and SEC-02 may need `happy-dom` or the test may need to use `--dom` flag. Add a note about running with `bun test --dom` if needed.

**Test groups:**

1. **"basic parsing (EML-01)"**
   - Load simple.eml as string
   - `const msg = await parseEml(emlString)`
   - Expect msg.content to be defined
   - Expect msg.attachments to be an array
   - Expect msg.recipients to be an array

2. **"header extraction (EML-02)"**
   - Expect msg.content.subject === 'Test Email Subject'
   - Expect msg.content.senderName === 'John Doe'
   - Expect msg.content.senderEmail === 'john@example.com'
   - Expect msg.content.date to be instance of Date
   - Expect msg.content.date.getFullYear() === 2026
   - Expect msg.recipients to include { name: 'Jane Smith', email: 'jane@example.com', type: 'to' }
   - Expect msg.recipients to include one with type: 'cc'

3. **"RFC 2047 decoding (EML-03)"**
   - Load encoded-headers.eml
   - Expect subject to contain 'Caf' (decoded from QP, should be 'Cafe menu' or similar decoded form)
   - Expect subject NOT to contain '=?UTF-8?'
   - Expect senderName to contain decoded Cyrillic (not '=?UTF-8?B?')

4. **"plain text body (EML-04)"**
   - Expect msg.content.body to contain 'plain text body'

5. **"HTML body with sanitization (EML-05, SEC-01)"**
   - Load multipart.eml
   - Expect bodyHTML to contain '<b>World</b>' or 'World' (bold preserved)
   - Expect bodyHTML to contain '<table' (tables preserved)
   - Expect bodyHTML NOT to contain '<script' (stripped)
   - Expect bodyHTML NOT to contain 'javascript:' (stripped)
   - Expect bodyHTML NOT to contain 'alert' from script tag

6. **"multipart/alternative preference (EML-06)"**
   - Expect bodyHTML to be non-empty (HTML was available)
   - Expect body (plain text) also to be non-empty

7. **"attachment extraction (EML-07)"**
   - Load multipart-mixed.eml
   - `const mixedMsg = await parseEml(multipartMixedEml)`
   - Expect mixedMsg.attachments.length to be >= 1
   - Expect mixedMsg.attachments[0].filename to equal 'hello.txt'

8. **"remote image blocking (SEC-02)"**
   - Expect bodyHTML NOT to contain 'https://tracker.example.com'
   - Expect bodyHTML to contain 'data:image/png' (data URIs preserved)

9. **"bodyRTF is undefined for EML"**
   - Expect msg.content.bodyRTF to be undefined

10. **"graceful degradation"**
    - Expect `await parseEml('From: test@test.com\r\n\r\n')` to NOT throw
    - Expect result to have content.subject === '' and content.body (empty or fallback)

Run with: `bun test tests/eml-parser.test.ts`

If DOMPurify requires DOM, run with: `bun test --dom tests/eml-parser.test.ts`

Add a `package.json` scripts entry if one doesn't exist: `"test": "bun test --dom"`
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Projects/msg-viewer && bun test --dom tests/eml-parser.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/eml-parser.test.ts` exists with at least 9 test cases
    - Test imports parseEml from `../lib/scripts/eml/eml-parser`
    - Test loads .eml fixtures from `tests/fixtures/`
    - Tests cover: basic parsing, header extraction, RFC 2047 decoding, plain text body, HTML body, multipart/alternative, attachment extraction (multipart/mixed), sanitization (no script), remote image blocking, graceful degradation
    - All tests pass when run with `bun test --dom tests/eml-parser.test.ts`
    - Test file uses `describe`/`test`/`expect` from `bun:test`
  </acceptance_criteria>
  <done>All tests pass — parseEml correctly handles all requirement scenarios including attachment extraction and sanitization</done>
</task>

</tasks>

<verification>
- All tests in `tests/eml-parser.test.ts` pass
- Test coverage spans all 12 requirement IDs (EML-01 through EML-10, SEC-01, SEC-02)
- No test uses mocks for postal-mime or DOMPurify — tests exercise the real adapter
</verification>

<success_criteria>
- `bun test --dom tests/eml-parser.test.ts` exits with 0
- All requirement IDs validated by at least one test assertion
- .eml fixtures are reusable for future Phase 3 and Phase 4 testing
</success_criteria>

<output>
After completion, create `.planning/phases/02-eml-parser-core/02-03-SUMMARY.md`
</output>
