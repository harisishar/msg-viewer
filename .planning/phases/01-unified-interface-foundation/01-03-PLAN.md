---
phase: 01-unified-interface-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements: [UI-02]

must_haves:
  truths:
    - "A real .msg file renders in the browser after the migration"
    - "The rendered output shows subject, sender, date, recipients, and body"
    - "Embedded messages (if present in test file) open on click"
  artifacts: []
  key_links:
    - from: "lib/scripts/index.ts"
      to: "lib/components/message/index.ts"
      via: "parse() -> messageFragment() -> DOM"
      pattern: "messageFragment"
---

<objective>
Verify the migrated application works end-to-end by running the build and testing with a real .msg file.

Purpose: The entire phase is a regression-safe migration. This plan confirms the existing .msg viewer still works identically through the new UnifiedMessage pipeline. No code changes — just verification.
Output: Confirmed working build, human-verified rendering.
</objective>

<execution_context>
@/Users/abu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-unified-interface-foundation/01-01-SUMMARY.md
@.planning/phases/01-unified-interface-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and verify no errors</name>
  <files></files>
  <read_first>
    lib/scripts/index.ts
    lib/scripts/msg/msg-parser.ts
    lib/scripts/types/unified-message.ts
  </read_first>
  <action>
Run the full application build to confirm TypeScript compilation succeeds with the migrated code:

```bash
bun build lib/index.html --outdir /tmp/msg-viewer-verify
```

If the build succeeds, check the output for any warnings. If it fails, diagnose and fix the error — the issue is likely a missed import or type mismatch from the migration.

Additionally, verify no residual old-type imports remain anywhere in the rendering pipeline:

```bash
grep -r "from.*msg/types/message" lib/components/ lib/scripts/index.ts
grep -r "DirectoryEntry" lib/components/ lib/scripts/index.ts
```

Both grep commands should return zero results.
  </action>
  <verify>
    <automated>cd /Users/abu/Documents/Projects/msg-viewer && bun build lib/index.html --outdir /tmp/msg-viewer-verify 2>&1 && echo "BUILD_OK" && grep -rc "from.*msg/types/message" lib/components/ lib/scripts/index.ts 2>/dev/null | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `bun build lib/index.html --outdir /tmp/msg-viewer-verify` exits with code 0
    - grep for `from.*msg/types/message` in lib/components/ and lib/scripts/index.ts returns 0 matches
    - grep for `DirectoryEntry` in lib/components/ and lib/scripts/index.ts returns 0 matches
  </acceptance_criteria>
  <done>Full application build succeeds with zero TypeScript errors and zero residual old-type imports in the rendering pipeline</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify .msg rendering in browser</name>
  <files></files>
  <action>
Human verification checkpoint. No code changes — the user tests the built application in a browser with a real .msg file to confirm the migration preserved identical rendering behavior.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification of .msg rendering in browser"</automated>
  </verify>
  <done>Human confirms .msg rendering is identical to pre-migration behavior</done>
  <what-built>
    The entire MSG parser and UI component pipeline has been migrated from the old Message type to UnifiedMessage. The parser now returns UnifiedMessage natively with pre-parsed embedded messages, typed recipients, and Uint8Array attachment content. All five UI components consume UnifiedMessage without any format-specific types.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `bun run dev` (or open the built HTML directly from /tmp/msg-viewer-verify/)
    2. Open the application in a browser
    3. Load a .msg file using the file picker or drag-and-drop
    4. Verify:
       - Subject line displays correctly
       - Sender name and email display correctly
       - Date displays correctly
       - To and CC recipients display correctly (check that CC recipients are in the CC section, not mixed into To)
       - Message body (plain text or HTML) renders correctly
       - If the .msg has attachments: attachment names and sizes display, download links work
       - If the .msg has embedded messages: clicking the embedded message opens it correctly
    5. Compare with the behavior before this phase — rendering should be identical
  </how-to-verify>
  <resume-signal>Type "approved" if rendering is identical to before, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Full application build succeeds
- No old Message/DirectoryEntry types remain in the rendering pipeline
- Human confirms .msg rendering is identical to pre-migration behavior
</verification>

<success_criteria>
- Build succeeds with zero errors
- A real .msg file renders correctly in the browser with subject, sender, date, recipients, body, attachments, and embedded messages all working
- Human approves the rendering is identical to before the migration
</success_criteria>

<output>
After completion, create `.planning/phases/01-unified-interface-foundation/01-03-SUMMARY.md`
</output>
